---
- name: Reboot Windows and wait for connection
  ansible.windows.win_reboot:
    msg: "Windows maintenance reboot"
    pre_reboot_delay: 10
    post_reboot_delay: 30
    reboot_timeout: 1800
    connect_timeout: 10
    test_command: 'powershell.exe -NoProfile -Command "whoami"'
